# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/orion_db
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (para desenvolvimento)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JWT Configuration
jwt.secret=aB2cD4eF6gH8iJ0kL2mN4oP6qR8sT0uV2wX4yZ6aB8=
jwt.expiration=3600000

# Server Configuration
server.port=8080

# Delay para aguardar o banco ficar pronto
spring.datasource.initialization-fail-timeout=60000
spring.datasource.continue-on-error=true